// @flow
import { initialState as initialFirstHalfGoals } from '../../redux/modules/firsthalfgoals/reducers';

const initialState = {
  firstHalfGoals: initialFirstHalfGoals,
};

export default initialState;
